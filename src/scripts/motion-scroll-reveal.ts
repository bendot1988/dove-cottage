/**
 * Scroll reveal is intentionally disabled sitewide.
 * Keep this no-op export so existing page imports do not break.
 */
export function initMotionScrollReveal(rootSelector = ".motion-scope"): void {
  void rootSelector;
}
